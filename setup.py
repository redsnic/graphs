import setuptools

setuptools.setup(
   name='graphs',
   version='0.99',
   description='A simple library for basic graph operations',
   author='Nicolò Rossi',
   author_email='olocin.issor@gmail.com',
   install_requires=['wheel', 'numpy'],
   packages=setuptools.find_packages()
)